import React, {FC} from 'react';
import styles from './SliderNews.module.sass';
export const SliderNews: FC = () => {
	return (
		<div className={styles.root}>
			<span className={styles.tag}>Физика</span>
		</div>
	);
};
